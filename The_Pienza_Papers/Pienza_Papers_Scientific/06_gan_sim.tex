\section{Phase 6: Generative Moonshots for Scaling}

The objective of Phase 6 is to transition the Pienza framework from a single-agent behavioral study into a scale-invariant Digital Twin of Mexico City Marketplace. While the previous phases successfully cloned the agent's policy, the ground-truth volume ($N \approx 4,700$) represents a data-sparsity constraint for high-fidelity market simulation. This phase documents the architectural blueprint for overcoming this bottleneck through generative synthesis and sequential deep learning.

The scaling architecture is structured across four primary technical pillars:

\begin{itemize}[noitemsep, topsep=3pt]
    \item \textbf{Generative Synthesis:} Implementation of GANs to synthesize a 1,000,000-row market manifold, preserving the statistical correlations of the original ground truth.
   \item \textbf{Network Topology:} Mapping the operational environment as a non-Euclidean graph (nodes and edges) to enable future Graph Neural Network (GNN) positioning models.
    \item \textbf{Sequential Deep Learning:} Training Recurrent Neural Networks (RNNs) on distilled synthetic sequences to move from isolated events to "Market Momentum" modeling.
    \item \textbf{Stochastic Simulation:} Utilizing the manifold to execute \textit{Markov Chain Monte Carlo} (MCMC) simulations, identifying global optimal strategies across millions of potential session iterations.
 
\end{itemize}

\subsection{Generative Synthesis: The Pienza Manifold}
To overcome the constraints of data sparsity ($N \approx 4,700$), the project deployed a \textbf{Conditional Generative Adversarial Network (cGAN)} to synthesize a 1,000,000-row data manifold. Unlike standard generative models that learn a broad distribution, the \textbf{conditional architecture} allows the system to synthesize offers targeted at specific market states. By conditioning the model on attributes—such as \texttt{hour\_of\_day}, \texttt{product\_tier}, and \texttt{zone\_id}—the system can programmatically request synthetic trips for any desired operational scenario.

This implementation effectively transforms the model into an \textbf{Infinite Keras Generator}. Because the neural network functions as a continuous mathematical mapping, it is capable of streaming an unlimited volume of synthetic offers on-demand. While the generator provides this infinite capacity, a fixed 1,000,000-row data manifold was materialized as a static Parquet asset. 

Reaching convergence required seven iterations of feature selection and spatial tuning. To prevent \textbf{Mode Collapse} (a state where the model outputs repetitive, low-variance data), the trip architecture was partitioned into two orthogonal vectors: \textit{Context} and \textit{Physics}.

Initial attempts utilized Uber's H3 hexagonal grid at Resolution 6 ($\approx 36$ km\textsuperscript{2}) for pickup origins. This coarse resolution proved insufficient, conflating distinct strategic sectors. Conversely, shifting to a hyper-granular Resolution 9 was a recipe for overfitting, forcing the model to memorize specific coordinates rather than learning urban distributions. 

\begin{figure}[H]
     \centering
     \begin{subfigure}[b]{0.32\textwidth}
         \centering
         \includegraphics[width=\textwidth]{geo_entropy.png}
         \caption{Geospatial Entropy.}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.32\textwidth}
         \centering
         \includegraphics[width=\textwidth]{h3_overlay.png}
         \caption{Res. 9 vs. Polygon Alignment.}
     \end{subfigure}
     \hfill
     \begin{subfigure}[b]{0.32\textwidth}
         \centering
         \includegraphics[width=\textwidth]{pickup_heatmap.png}
         \caption{Ground Truth Heatmap.}
     \end{subfigure}
        \caption{Geospatial Discovery: Identifying the resolution trap between algorithmic hexagonal grids and strategic operational boundaries.}
        \label{fig:geo_discovery}
\end{figure}

This was resolved by executing a \textbf{Point-in-Polygon (PiP)} spatial join. By mapping raw GPS pickups directly to the 42 semantic macro-zones developed in Phase 4, the system unified the spatial axis. This allowed the GAN to learn origin-destination economics without the noise of an abstract grid.

While the ideal model would replicate transient incentives (\textit{e.g., Surge, Turbo+}), these high-variance signals derailed the learning of baseline prices, as these features collapsed to zero after training. To prevent mode collapse and stabilize the Generator, incentives were excised from the feature space; the remaining variables were split into two tensors:

\begin{itemize}[noitemsep, topsep=3pt]
    \item \textbf{Context Switches (Conditioning):} The state of the world prior to the request (\textit{e.g., Hour of Day, Day of Week, Product Tier, Pickup/Dropoff Zone}).
    \item \textbf{Pure Physics (Generated):} The continuous variables the model must hallucinate (\textit{e.g., Upfront Fare, Estimated Duration, Trip Distance, Pickup Details}).
\end{itemize}

To stabilize the model's activations and ensure realistic outputs, three constraints were applied:
\begin{itemize}[noitemsep]
    \item \textbf{Log-Transformations:} Applied to heavily skewed features (\textit{Fare, Distance}) to stabilize gradients during backpropagation.
    \item \textbf{MinMax Scaling $[-1, 1]$:} Compresses all physical features uniformly to prevent high-magnitude metrics (\textit{e.g., seconds}) from mathematically overwhelming smaller scalars (\textit{e.g., km}).
    \item \textbf{Oversampling:} \textit{Premium} tier trips represent a minuscule fraction of the raw data. To prevent tier blindness, a $15 \times$ oversampling multiplier was applied exclusively to this category, forcing the network to assimilate high-value, low-frequency market behaviors.
\end{itemize}


\subsubsection{Neural Architecture: The Generator Pipeline}
The Generator is architected as a conditional dense network designed to transform real observed data into synthetic,  operationally-valid, ride offers. As illustrated in Figure \ref{fig:generator_arch}, the model employs a four-stage funnel to synthesize market physics from categorical constraints.



\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{fig_generator_v7.jpg} 
    \caption{Technical Architecture of the Pienza Generator v7.}
    \label{fig:generator_arch}
\end{figure}

\subsubsection{Neural Architecture: The Generator Pipeline}
The Pienza Generator (v7) utilizes an embedding-concatenation strategy to map market constraints to physical outcomes. The six categorical variables at the input stage (\textit{e.g., temporal blocks, product tier, geospatial zones}) act as \textbf{conditional switches} rather than synthesized outputs. When the system is prompted with a specific market context---such as an 8:00 AM mission on a Monday from Zone A to Zone B---the model's 2.7 million parameters synthesize the exact continuous physics (fare, durations, and distances) that correspond to those specific constraints.

To ensure stochastic variety and prevent repetitive outputs, 100 dimensions of Gaussian noise are fused with the embedded context vectors. This 133-dimensional input is then expanded through a series of three progressive \textbf{Dense Blocks} (ranging from 512 to 2048 neurons). Each block utilizes \textit{Batch Normalization} and \textit{LeakyReLU} activations to maintain mathematical stability during the adversarial training process. The architecture terminates in a five-neuron \textbf{\textit{tanh} output layer}, which generates the normalized values for the core physics metrics. These are subsequently mapped back to real-world units via the inverse transformation of the data refinery's scaling protocol.

\noindent \textit{Outcome: This architecture allows for the on-demand generation of an infinite stream of statistically valid ride offers, providing the high-volume data manifold required for the subsequent simulation and deep learning phases.}

\subsubsection{Discriminator Architecture: The Adversarial Validator}
The Discriminator (v7) serves as the adversarial validator within the cGAN framework. Its primary objective is to perform a binary classification task, distinguishing between ground-truth operational records and synthetic hallucinations. To provide the Generator with effective gradient pressure, the Discriminator is architected to evaluate both the physical magnitude of the offer and its environmental context.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{discri2.jpg} 
    \caption{Architecture of the Pienza Discriminator v7.}
    \label{fig:discriminator_arch}
\end{figure}

The architecture utilizes a three-stage validation logic to render its verdict:

\begin{enumerate}
    \item \textbf{Contextual Fusion:} The network ingests the six strategic switches (\textit{e.g., temporal blocks, zone IDs}) through embedding layers. These categorical vectors are flattened and concatenated with the 5-dimensional physics input. This creates a unified 38-dimensional feature vector, ensuring the model evaluates the validity of the fare and duration \textbf{conditional} upon the specific market state.
    
    \item \textbf{Regularized Funnel:} The concatenated signal is processed through a descending dense stack of 1024, 512, and 256 neurons. To prevent the Discriminator from reaching premature convergence---which would stall the Generator's learning---the architecture implements aggressive \textbf{Dropout} layers ($p=0.2$) following each \textit{LeakyReLU} activation. This forces the model to prioritize robust statistical patterns over specific coordinate memorization.
    
    \item \textbf{The Verdict:} The network terminates in a single-neuron output layer. This head utilizes a sigmoid activation to produce a probability score representing the model's confidence in the sample's authenticity.
\end{enumerate}

\noindent \textit{Outcome: With approximately 697,000 trainable parameters, the v7 Discriminator provides the necessary logic to identify subtle non-linear discrepancies in pricing physics. This adversarial tension ensures that the infinite stream generated by the system maintains 1:1 statistical parity with the original Mexico City operational environment.}


\subsubsection{Training Strategy and Optimization}
Training was executed via synchronized Adam optimizers ($LR=0.0002, \beta_1=0.5$) using a $1.0$ gradient clipping threshold to ensure numerical stability. To prevent the Discriminator from prematurely dominating the adversarial process, a $0.1$ label smoothing factor is applied to the Binary Cross-Entropy (BCE) loss function. The training loop is compiled as a \texttt{@tf.function} graph with a batch size of 64, allowing for efficient backpropagation across the physical and categorical tensors while preserving the statistical correlations of the original marketplace. The architecture was trained over a 1,000-epoch cycle to establish the baseline physical manifold. As illustrated in the loss curves (Figure \ref{fig:gan_convergence}), the adversarial process reached a stable equilibrium within 19.11 minutes.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{gan_convergence.png} 
    \caption{Training Loss Progression. The convergence of the Generator and Discriminator losses indicates a balanced adversarial state, preventing mode collapse and ensuring the synthetic variables remain within realistic operational bounds.}
    \label{fig:gan_convergence}
\end{figure}

The final performance metrics (Table \ref{tab:gan_results}) demonstrate high training stability. The proximity of the generator and discriminator losses confirms that the system has successfully mapped the underlying marketplace distributions without yielding to numerical instability.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{l l}
        \toprule
        \textbf{Metric} & \textbf{Final Value} \\
        \midrule
        Epochs Completed & 1,000 / 1,000 \\
        Total Training Time & 19.11 minutes \\
        Final Generator Loss & 1.0268 \\
        Final Discriminator Loss & 1.2039 \\
        \bottomrule
    \end{tabular}
    \caption{Performance summary for the Pienza V5 training cycle.}
    \label{tab:gan_results}
\end{table}

\noindent \textit{Outcome: The V5 model provides a statistically reliable foundation for the simulation environment. By prioritizing the stability of physical variables over high-variance incentives, the framework ensures a high degree of fidelity for the subsequent strategy testing phases.}


\subsection{Statistical Parity Assessment: Manifold Integrity}
To verify the generative model's fidelity, the 1,000,000 synthetic observations were benchmarked against the original ground-truth distributions. This assessment serves as a statistical validation to confirm that the cGAN successfully mapped the non-linear correlations of the Mexico City marketplace.

\subsubsection{Financial and Strategic Distribution}
The model's ability to replicate the economic hierarchy and decision frequency is illustrated in Figure \ref{fig:manifold_financial}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{fig_manifold_financial.png} 
    \caption{Financial Physics and Strategic Composition. The top row illustrates near-total overlap in the Probability Density Functions (PDF) for \texttt{upfront\_fare} and \texttt{est\_trip\_distance}. The bottom row confirms the model correctly learned the yield hierarchy across product tiers and the relative frequency of the \texttt{reason\_primary} classes.}
    \label{fig:manifold_financial}
\end{figure}

As shown in Figure \ref{fig:manifold_financial}, the synthetic data (Purple) exhibits a high degree of parity with the ground truth (Teal). The model successfully captured the long-tail distribution of the fare structure and the specific strategic composition of the Agent's decision set, ensuring that the simulated universe respects the original decision boundaries.

\subsubsection{Logistical Alignment and Temporal Rhythms}
Beyond pricing, the manifold must adhere to the physical constraints of the urban environment, specifically the relationship between time and distance.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{fig_manifold_logistics.png} 
    \caption{Logistical Integrity and Weekly Value Pulse. The scatter plot (bottom-left) demonstrates that the GAN captured the \textit{Approach Velocity} (Time vs. Distance to Pickup) with regression slopes identical to the ground truth. The bar chart (bottom-right) confirms the model internalized the weekly revenue cycles.}
    \label{fig:manifold_logistics}
\end{figure}

The logistical assessment (Figure \ref{fig:manifold_logistics}) verifies that the distributions of Time-to-Pickup (TTP) and Distance-to-Pickup (DTP) remain operationally valid. Furthermore, the model correctly identified Friday as the peak-revenue node in the weekly cycle, confirming the internalization of macroeconomic rhythms.

\subsubsection{The Circadian Pulse: Temporal High-Fidelity}
The final validation gate involved the replication of the \textbf{Circadian Value Pulse}---the sharp fluctuations in average fare across a 24-hour window.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{fig_manifold_circadian.png} 
    \caption{Circadian Value Pulse Verification. The manifold successfully replicates the sharp operational discontinuities observed at 09:00, the midday demand collapse, and the 19:00 peak.}
    \label{fig:manifold_circadian}
\end{figure}

As illustrated in Figure \ref{fig:manifold_circadian}, the generator accurately maps the market's non-linear temporal shifts. By respecting these sharp discontinuities, the manifold is certified as a representative environment for high-scale strategy testing and counterfactual analysis.

\noindent \textit{Outcome: The manifold achieves 1:1 statistical parity across all major operational dimensions. The synthetic dataset is hereby validated as the definitive environment for the Phase 6 simulation engine.}

\vspace{0.4cm}
\rule{\linewidth}{0.4pt}





To be Continued....





