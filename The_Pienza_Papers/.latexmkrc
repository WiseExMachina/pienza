# Keep all build byproducts (aux/log/bbl/pdf/etc.) out of the source
# directory. Input resolution (\input, \includegraphics, \addbibresource)
# is unaffected -- it's relative to the current working directory, not
# this output directory.
$out_dir = 'build';
$pdf_mode = 1;
