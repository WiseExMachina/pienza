{
  "name": "Pienza 3.11 High Performance",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  
  "containerEnv": {
    "PIP_BREAK_SYSTEM_PACKAGES": "1"
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgdal-dev libproj-dev g++ python3-dev && pip3 install --no-cache-dir -r requirements.txt && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*",

  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.terminal.activateEnvironment": false
      }
    }
  }
}